---
# Generated by LLM. I don't care about this one and I won't test it
- name: Add Insync repository
  become: true
  deb822_repository:
    name: insync
    description: Insync package repository
    architectures:
      - amd64
    uris:
      - http://apt.insync.io/ubuntu
    suites:
      - "{{ ansible_lsb.codename }}"
    components:
      - non-free
      - contrib
    signed_by:
      keyid: ACCAF35C
      url: "https://keyserver.ubuntu.com/pks/lookup?op=get&search=0xACCAF35C"
    state: present
    update_cache: yes

- name: Install
  become: true
  apt:
    name: insync
    state: present
